<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.printerbridge</groupId>
    <artifactId>printerbridge</artifactId>
    <version>1.1.0</version>
    <packaging>jar</packaging>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <javalin.version>7.2.2</javalin.version>
        <jackson.version>2.22.1</jackson.version>
        <jserialcomm.version>2.11.4</jserialcomm.version>
        <logback.version>1.5.38</logback.version>
        <junit.version>6.1.2</junit.version>
        <pdfbox.version>3.0.8</pdfbox.version>
        <!-- Tests tagged "hardware" hit real OS/Bluetooth discovery (WMI on Windows, rfcomm/bluetoothctl
             on Linux) - measured at ~7.5s per call on real hardware (the WMI enumeration itself, not a
             code issue), which made `mvn test` take minutes and made unrelated tests time out. Excluded
             from the default `mvn test` run; opt in with `mvn test -Dtest.excludedGroups=`. -->
        <test.excludedGroups>hardware</test.excludedGroups>
    </properties>

    <dependencies>
        <!-- Framework HTTP/WebSocket exposant l'API locale (couche API, cf. CLAUDE.md) -->
        <dependency>
            <groupId>io.javalin</groupId>
            <artifactId>javalin</artifactId>
            <version>${javalin.version}</version>
        </dependency>

        <!-- Implémentation SLF4J requise par Javalin/Jetty pour émettre leurs logs -->
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
            <version>${logback.version}</version>
        </dependency>

        <!-- Sérialisation JSON des réponses HTTP ; Javalin l'utilise automatiquement dès qu'elle est sur le classpath -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <!-- Accès aux ports Bluetooth RFCOMM (imprimantes thermiques) -->
        <dependency>
            <groupId>com.fazecast</groupId>
            <artifactId>jSerialComm</artifactId>
            <version>${jserialcomm.version}</version>
        </dependency>

        <!-- Rendu des PDF en Printable : le spouleur Windows ne supporte pas nativement le flavor PDF de javax.print -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>${pdfbox.version}</version>
        </dependency>

        <!-- Tests -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- Compile avec l'option release, cf. maven.compiler.release ci-dessus -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.15.0</version>
            </plugin>
            <!-- Exécute les tests JUnit lors de `mvn test` -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.5.6</version>
                <configuration>
                    <!-- jSerialComm charge une lib native (JNI) ; évite le warning JEP 472 -->
                    <argLine>--enable-native-access=ALL-UNNAMED</argLine>
                    <excludedGroups>${test.excludedGroups}</excludedGroups>
                </configuration>
            </plugin>
            <!-- Jar exécutable (Main-Class) ; utilisable seul via `java -jar`, jpackage la traite comme main-jar -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.5.0</version>
                <configuration>
                    <archive>
                        <manifest>
                            <mainClass>org.printerbridge.Main</mainClass>
                        </manifest>
                        <manifestEntries>
                            <!-- jSerialComm : évite le warning JEP 472 aussi en lancement `java -jar` -->
                            <Enable-Native-Access>ALL-UNNAMED</Enable-Native-Access>
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
            <!-- Rassemble les dépendances runtime à plat dans jpackage-input, aux côtés du jar principal -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.11.0</version>
                <executions>
                    <execution>
                        <id>copy-runtime-dependencies</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                            <includeScope>runtime</includeScope>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <!-- Copie le jar principal aux côtés de ses dépendances, pour que jpackage pointe vers un seul dossier d'entrée -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <version>3.5.0</version>
                <executions>
                    <execution>
                        <id>copy-main-jar-for-jpackage</id>
                        <phase>package</phase>
                        <goals>
                            <goal>copy-resources</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/jpackage-input</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${project.build.directory}</directory>
                                    <includes>
                                        <include>${project.build.finalName}.jar</include>
                                    </includes>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
